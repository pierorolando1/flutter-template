import 'package:flutter/material.dart';

class JuiceAppScreen extends StatelessWidget {
  const JuiceAppScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Text("Hola mundo"),
      ),
    );
  }
}
